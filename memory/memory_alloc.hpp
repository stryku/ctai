#pragma once

#include "memory/memory.hpp"

#include <cstddef>

namespace ctai
{
    namespace details
    {
        template<typename metadata, size_t size>
        struct memory_alloc_impl;

        template<typename metadata, size_t size>
        struct memory_alloc_impl;
    }
}
